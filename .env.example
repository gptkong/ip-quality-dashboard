# Vercel Postgres 连接字符串
# 在 Vercel Dashboard -> Storage -> Postgres 中获取
DATABASE_URL=postgres://user:password@host:5432/database

# API 鉴权 Token
# 用于保护 POST /api/servers 接口
# 请求时需要在 Header 中添加: Authorization: Bearer <token>
API_AUTH_TOKEN=your-secret-token-here

# Admin 管理后台密码
ADMIN_PASSWORD=your-admin-password-here
